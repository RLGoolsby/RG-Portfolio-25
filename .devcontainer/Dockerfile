dockerfile# Use the official Microsoft Dev Container base image for Node.js 22
FROM ://microsoft.com

# [Optional] Install additional OS packages if your frontend stack ever needs them (e.g., graphics packages)
# RUN apt-get update && export DEBIAN_FRONTEND=noninteractive \
#     && apt-get -y install --no-install-recommends <your-package-list-here>

# Set the default shell to bash
ENV SHELL=/bin/bash